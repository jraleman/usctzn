import React from 'react';
import styled from 'styled-components';

const AudioPlayer = () => {
    const label = 'AudioPlayer';
    return (
        <div>
            {label}
        </div>
    );
};

export default AudioPlayer;
